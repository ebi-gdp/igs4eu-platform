# Service name
serviceName: pipeline-manager

# Namespace
namespace: intervene-prod

# Environment-specific variables
env:
  profile: "gcp-prod-env"

# Replica count for development
replicaCount: 1

# Image details
image:
  repository: dockerhub.ebi.ac.uk/gdp/igs4eu-platform
  tagPrefix: pipeline-manager
  tag: 5.3.4
  tagPostfix: prod
  pullPolicy: Always

# Service account
serviceAccount: gcp-service-api

# Application properties file
properties:
  fileName: "application-gcp-prod-env.properties"

# Kubernetes resource limits for development
resources:
  limits:
    cpu: "1"
    memory: "1000Mi"
  requests:
    cpu: "0.5"
    memory: "500Mi"

# Java jvm memory size
java:
  maxSize: -Xmx768m

# GCP project details
gcp:
  projectName: "prj-ext-prod-intervene-437413"
  zone: "europe-west2"
  dbName: "intervene-prod"

# GCP cloud sql proxy resource
cloudSql:
  resources:
    limits:
      cpu: "1"
      memory: "500Mi"
    requests:
      cpu: "0.5"
      memory: "300Mi"

