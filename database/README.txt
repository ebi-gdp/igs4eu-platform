# Detailed documentation has been mentioned on Confluence to set up & create database. Once you set up database on GCP, execute these scripts.

# Database names are dev => intervene, prod => intervene-prod & test can be test => intervene-test

1. There are total 2 sql files to set up PostgreSQL database.

2. intervene-1.sql & intervene-2.sql => Run commands in this file one after another to create tables, data types (enums), stored procedure, sequence etc.
