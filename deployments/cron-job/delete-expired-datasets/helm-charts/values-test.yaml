namespace: intervene-test

# username=pipeline-manager basic auth username
# password=pipeline-manager basic auth password
# This is base64(Basic base64(username:password))
basicAuth: QmFzaWMgZEdWemREcDBaWE4w
